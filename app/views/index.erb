
<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Robot World Index</title>
  </head>
  <body>
    <h1 class="row" >Here is all of the Robots currently on Robot World</h1>
      <% @robots.each do |robot| %>
        <h3> <%= robot.id %>: <a href="/robots/<%= robot.id %>"><%= robot.name %></a></h3>
        <a class="btn btn-default" href="/robots/<%= robot.id %>/edit" role="button">Edit this Task</a>
        <form action="/robots/<%= robot.id %>" method="post">
          <input type="hidden" name="_method" value="delete">
          <input type="submit" value="Delete this Robot" class="delete-button">
        </form>
      <% end %>
  </body>
</html>
